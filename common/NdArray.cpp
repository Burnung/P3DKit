#include "P3D_Utils.h"
#include "NdArray.h"
//#include "image_impl_cpu.h"
//#include "ndarray_impl_1.h"
#include <algorithm>
//#include "Transform2DUtil.h"
using namespace std;

#define doF(i, n) for(int i = 0; i < n; i++)
#define doFs(i, j, n) for(int i = j; i < n; i++)
#define doFv(i, vs) for(int i = 0; i < vs.size(); i++)
#define doFvs(i, j, vs) for(int i = j; i < vs.size(); i++)

BEGIN_P3D_NS


END_P3D_NS
